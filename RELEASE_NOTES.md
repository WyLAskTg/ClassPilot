## ClassPilot v1.0.1

- Installer assets now use lowercase English filenames, such as `classpilot-setup-1.0.1.exe`.
- The installer finish page now lets users choose whether to create a desktop shortcut.
- The app, shortcut, and uninstall names stay `ClassPilot` in both installer languages.
